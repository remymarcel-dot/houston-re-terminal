// Copy this file to config.js and fill in your keys.
// config.js is gitignored — NEVER commit real keys.
window.CONFIG = {
  // Free tier (50 calls/mo): https://app.rentcast.io/app/api
  RENTCAST_KEY: "",
};
